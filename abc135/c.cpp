#include <bits/stdc++.h>
using namespace std;

int main() {
    long long n; cin >> n;
    vector<int> a(n+1); for (int i=0; i<n+1; ++i) cin >> a[i];
    vector<int> b(n); for (int i=0; i<n; ++i) cin >> b[i];
    
}