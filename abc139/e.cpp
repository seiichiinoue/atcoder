#include <bits/stdc++.h>
typedef pair<long long, long long> P;
#define rep(i,n) for(int i=0; i<n; ++i)
using namespace std;


int main() {
    long long N; cin >> N;
    long long days = 0;
    UnionFind(N*(N-1)/2);
    vector<P> tmp;
    vector<long long> used_list;
    for (int i=0; i<N; ++i) {
        for (int j=0; j<N-1; ++j) {

        }
    }
}